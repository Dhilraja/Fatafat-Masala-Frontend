services:
  - type: static
    name: Fatafat-Masala
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/fatafat-masala/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
